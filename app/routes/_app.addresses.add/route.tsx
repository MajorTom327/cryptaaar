import { Outlet } from "react-router";
export { ErrorBoundary } from "~/components/error-boundary";

export const AddressesAddRoute = () => {
  return <Outlet />;
};

export default AddressesAddRoute;
